input {
  jdbc {
    jdbc_connection_string =>"jdbc:postgresql://${DB_HOST_DEV}/content_translation_work"
    jdbc_user => "${DB_USER}"
    # jdbc_password => "${DB_PASS}"
    jdbc_password => "dev"
    jdbc_driver_class => "org.postgresql.Driver"
    schedule => "0 2 */2 * * *"
    # schedule => "0/10 * * * * *"
    statement => 'SELECT * FROM public."gloqual_feedbacks_view" WHERE "severityLevel" is not null'
    # statement => 'SELECT * FROM public."gloqual_feedbacks_view" WHERE "qualityPoint" is not null'
    # statement => 'SELECT * FROM public."all_content_translation_work_view" where "updatedAt" > current_date-1'
  }
}
output {
  elasticsearch {
    hosts => ["https://gloz-47a7f3.es.ap-northeast-2.aws.elastic-cloud.com:443"]
    # action => "create"
    index => "gloqual_feedbacks_view"
    document_id => "%{feedbackid}"
    user => "${ES_USER}"
    password => "${ES_PASS}"
  }
}