input {
  jdbc {
    jdbc_connection_string =>"jdbc:postgresql://${DB_HOST}/${DB_NAME}"
    jdbc_user => "${DB_USER}"
    jdbc_password => "${DB_PASS}"
    jdbc_driver_class => "org.postgresql.Driver"
    schedule => "0 0 */6 * * *"
    statement => 'SELECT * from public."NetflixDttOriginationSummary"'
  }
}
output {
  elasticsearch {
    hosts => ["https://gloz-47a7f3.es.ap-northeast-2.aws.elastic-cloud.com:443"]
    action => "create"
    index => "netflix-dtt_origination_summary"
    document_id => "%{originationsummarysheetid}_%{originationsummarysheetrownumber}"
    user => "${ES_USER}"
    password => "${ES_PASS}"
  }
}