input {
  jdbc {
    jdbc_connection_string =>"jdbc:postgresql://${DB_HOST}/${DB_NAME}"
    jdbc_user => "${DB_USER}"
    jdbc_password => "${DB_PASS}"
    jdbc_driver_class => "org.postgresql.Driver"
    #schedule => "0/5 * * * * *"
    schedule => "0 0 */6 * * *"
    statement => 'SELECT * from public."all_assetqc_view"'
  }
}
output {
  elasticsearch {
    hosts => ["https://gloz-47a7f3.es.ap-northeast-2.aws.elastic-cloud.com:443"]
    action => "create"
    index => "netflix-dtt_all_assetqc_view"
    document_id => "%{assetqcresultssheetid}_%{assetqcresultssheetrownumber}_%{assetqcerrorssheetrownumber}"
    user => "${ES_USER}"
    password => "${ES_PASS}"
  }
}